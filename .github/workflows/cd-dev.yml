# Description:
# Publish the latest build to the internal Plugin Catalog (dev environment), then trigger
# an Argo CD workflow to deploy the published artifact.
# Intended as a main-branch-only CD pipeline; callers are responsible for only invoking
# on pushes to main.

name: Plugins - Publish to Dev Catalog and Deploy

on:
  workflow_call:
    inputs:
      go-version:
        description: Go toolchain version to use.
        type: string
        required: true
      golangci-lint-version:
        description: golangci-lint version to use.
        type: string
        required: true
      environment:
        description: Plugin Catalog environment to publish to.
        type: string
        required: false
        default: "dev"
      scopes:
        description: |
          Publishing scope — the Grafana Cloud instance the plugin is provisioned into.
          Used to restrict where the artifact is published.
        type: string
        required: false
        default: "grafana_cloud"
      argo-instance:
        description: Argo CD instance to target.
        type: string
        required: false
        default: "ops"
      argo-namespace:
        description: Argo CD namespace where the WorkflowTemplate lives.
        type: string
        required: false
        default: "grafana-datasources-cd"
      plugin-directory:
        description: |
          Directory containing plugin.json and package.json, relative to the repo root.
          Defaults to src, which is the standard layout for Grafana datasource plugins.
        type: string
        required: false
        default: "src"

defaults:
  run:
    shell: bash

# Prevent duplicate runs for the same ref. Do not cancel in-progress runs —
# a deploy already underway should finish rather than be left in a partial state.
concurrency:
  group: cd-dev-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  publish-to-catalog:
    name: Publish to Dev Catalog
    # Skip when Renovate auto-merges — those commits don't need a dev catalog deployment.
    if: ${{ github.actor != 'renovate-sh-app[bot]' }}
    uses: grafana/plugin-ci-workflows/.github/workflows/cd.yml@ci-cd-workflows/v11.1.0 # zizmor: ignore[unpinned-uses]
    permissions:
      attestations: write
      contents: write
      id-token: write
      pull-requests: read
    with:
      disable-docs-publishing: true
      disable-github-release: true
      environment: ${{ inputs.environment }}
      go-version: ${{ inputs.go-version }}
      golangci-lint-version: ${{ inputs.golangci-lint-version }}
      plugin-version-suffix: ${{ github.sha }}
      run-playwright: false
      scopes: ${{ inputs.scopes }}

  trigger-argo-workflow:
    name: Trigger Argo Workflow
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    needs: publish-to-catalog
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Read plugin ID
        id: plugin
        env:
          PLUGIN_DIR: ${{ inputs.plugin-directory }}
        run: |
          set -euo pipefail
          echo "id=$(jq -er .id "./${PLUGIN_DIR}/plugin.json")" >> "$GITHUB_OUTPUT"

      - name: Read package version
        id: pkg
        run: |
          set -euo pipefail
          echo "version=$(jq -er .version ./package.json)" >> "$GITHUB_OUTPUT"

      - name: Trigger workflow
        uses: grafana/shared-workflows/actions/trigger-argo-workflow@5a212d56e9d224851dc4b3c2643bbc5bac484d5d # trigger-argo-workflow/v2.0.0
        with:
          instance: ${{ inputs.argo-instance }}
          namespace: ${{ inputs.argo-namespace }}
          workflow_template: ${{ steps.plugin.outputs.id }}-${{ inputs.environment }}
          parameters: |
            dockertag=${{ steps.pkg.outputs.version }}+${{ github.sha }}
            prCommentContext=triggered-by-push-to-main datasource=${{ steps.plugin.outputs.id }}
            commit_author=grafana-delivery-bot
